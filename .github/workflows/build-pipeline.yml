name: Build pipeline
on: 
  push:
    branches: [ $default-branch ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build artifact"
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test Artifact"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ecoh "Deploy"


